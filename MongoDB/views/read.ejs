<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 text-white p-10">
      <div class="my-5">
        <a href="/" class="text-zinc-500">Back to Home</a>
      </div>
      <h1 class="text-3xl mb-3">All Users</h1>
      <div class="users flex gap-5 flex-wrap">
        <% if (users.length > 0){ %> <% users.forEach((function(user){ %>
        <div class="user p-6 bg-zinc-800 w-72 rounded-md">
          <div class="w-full h-72 bg-zinc-500 rounded-md overflow-hidden">
            <img
              src="<%= user.image%>"
              class="w-full h-full object-cover"
              alt=""
            />
          </div>
          <p class="text-2xl mt-5"><%= user.name %></p>
          <p class="text-zinc-500"><%= user.email%></p>
          <div class="flex gap-3 mt-3">
            <a href="/edit/<%= user._id %>" class="bg-blue-500 p-2 rounded-md"
              >Edit User</a
            >
            <a href="/delete/<%= user._id%>" class="bg-red-500 p-2 rounded-md"
              >Delete User</a
            >
          </div>
        </div>
        <%}))%> <% } else {%>
        <h3 class="text-zinc-500 text-3xl">No User Yet</h3>
        <% }%>
      </div>
    </div>
  </body>
</html>
